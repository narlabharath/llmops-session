# =============================================================================
# llmops-session — core dependencies
#
# Optional / per-lesson deps (langgraph, ragas, llm-guard, langfuse, etc.)
# are added when their notebook needs them. See dev-docs/UNDERSTANDING.md §5.
# =============================================================================

# --- Config & data -----------------------------------------------------------
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
pydantic>=2.0.0
pyyaml>=6.0

# --- Notebooks ---------------------------------------------------------------
jupyter>=1.0.0
nbconvert>=7.0.0
ipykernel>=6.0.0

# --- LLM providers -----------------------------------------------------------
anthropic>=0.40.0
openai>=1.50.0
google-generativeai>=0.8.0

# --- RAG ---------------------------------------------------------------------
langchain-core==0.3.86
langchain-community==0.3.31
langchain-text-splitters==0.3.11
langchain-chroma==0.1.4
langchain-huggingface==0.1.2
chromadb==0.5.23
sentence-transformers==3.4.1

# --- App ---------------------------------------------------------------------
streamlit>=1.35.0

# --- Testing -----------------------------------------------------------------
pytest>=8.0.0

# --- CLI / output ------------------------------------------------------------
rich>=13.0.0
